// Globals


// Mutable Vars
let gridSize = 16; //cells per row

// Queries
const sketchContainer = document.querySelector('#container');

// Functions
function partitionContainer (width, height) {

    


    return sizePerBox;
}

// Code
partitionContainer(sketchContainer.clientWidth, sketchContainer.clientHeight);